#pragma once

/*
 * Assume everything that doesn't have a optional is mandatory to include
 * in code.
 */

#include <iostream>
#include <string>
#include <vector>

namespace Compiler {

class Parser {
public:
};
} // namespace Compiler
